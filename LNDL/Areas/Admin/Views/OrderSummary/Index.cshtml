@model IEnumerable<LNDLWcfService.CodeFirstEntities.OrderFromCustomer>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.orderNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.orderIssueDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.orderExpectedDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.orderNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.orderIssueDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.orderExpectedDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
